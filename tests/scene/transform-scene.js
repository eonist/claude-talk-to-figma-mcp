// model transform-scene.js after shape-scene.js

// Lets make a few calls 

// create_rectangle(w,x,w,h,fillColor)
    // create the rectangle at 0,0,100,100
    // then set_position
    // then set_size

// create 3 rectangles in a horizontal row (red,green,blue)

// create 3 rectangles with different w,h (red,green,blue)
    // place them inside a frame
    // add horizontal autolayout with wrap (add padding and gaps)
    // reorder z position so that the rectangles now are ordered (blue,green,red)