import { generateCommandId } from "../utils.js";

/**
 * Apply font settings to multiple text nodes at once.
 *
 * @async
 * @function
 * @param {Object} params - Parameters for bulk font application (see intended implementation in font.js).
 * @throws {Error} Always throws, not yet implemented.
 */
export async function setBulkFont(params) {
  // For brevity, this is a stub. The full implementation should be moved from font.js,
  // and should import any helpers (sendProgressUpdate, delay, getFontStyle) from font-helpers.js.
  throw new Error("setBulkFont: Not yet implemented in modular split.");
}
